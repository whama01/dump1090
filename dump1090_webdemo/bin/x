

            select
             date_trunc('minute',max(msg_gen_ts))             as end_time,
             date_trunc('minute',max(msg_gen_ts) - interval '10 mins')  as start_time
           from
             dump1090_batch.dump1090_id
           \p\g

           
